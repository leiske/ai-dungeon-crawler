* use Bun standard library, not node
* prefer pure, well-scoped functions
* there is no such thing as backwards compatibility in this codebase
* when running the sim always use "--max-turns 5" to prevent wasting my tokens
* don't add "fallbacks"
